<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thank You - Order Placed</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .thank-you-container {
      min-height: 60vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }
    .success-icon {
      font-size: 5rem;
      color: #28a745;
      margin-bottom: 1rem;
    }
    .delivery-info {
      background: #f8f9fa;
      padding: 1.5rem;
      border-radius: 8px;
      margin: 2rem 0;
      max-width: 600px;
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>
  
  <div class="container">
    <div class="thank-you-container">
      <div class="success-icon">✓</div>
      <h1 class="display-4 mb-3">Payment confirmed</h1>
      <p class="lead text-muted">Thank you for your order<% if (paymentInfo && paymentInfo.name) { %>, <strong><%= paymentInfo.name %></strong><% } %>.</p>
      
      <% if (paymentInfo && paymentInfo.method) { %>
        <p class="note">Payment method: <strong><%= paymentInfo.method %></strong></p>
      <% } %>
      <% if (orderId) { %>
        <div class="alert alert-success mt-3" role="alert">
          <strong>Order ID:</strong> #<%= orderId %>
        </div>
      <% } %>

      <div class="mt-4">
        <% if (orderId) { %>
          <a href="/orderdetails?orderId=<%= orderId %>" class="btn btn-primary btn-lg me-2">
            View Order Details
          </a>
        <% } else { %>
          <a href="/orders" class="btn btn-primary btn-lg me-2">
            View My Orders
          </a>
        <% } %>
        <a href="/shopping" class="btn btn-outline-secondary btn-lg">
          Continue Shopping
        </a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
